The ``100-matrix_mul`` module
======================

Using ``matrix_mul``
-------------------
import module:
    >>> matrix_mul =  __import__("100-matrix_mul").matrix_mul

Normal case:
    >>> matrix = [[0, 0], [0, 0]]
    >>> matrix_mul(matrix, matrix)
    [[0, 0], [0, 0]]
